# Application Configuration
# These are the default values, copy to .env to customize

APP_TITLE=Feed — News Aggregator API
APP_VERSION=1.0.0

# Performance
REQUEST_TIMEOUT=10
CACHE_EXPIRY_SECONDS=1800
MAX_CACHE_ENTRIES=500

# Proxy Configuration (optional/future-use)
# PROXY_URL=
